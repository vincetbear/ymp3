[phases.setup]
nixPkgs = ["python39"]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "ffmpeg -version || echo 'WARNING: FFmpeg not found!'",
    "chmod +x start.sh"
]

[start]
cmd = "./start.sh"
