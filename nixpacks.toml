[phases.setup]
nixPkgs = ["python39"]

[phases.install]
cmds = [
    "apt-get update && apt-get install -y ffmpeg",
    "ffmpeg -version",
    "pip install -r requirements.txt",
    "chmod +x start.sh"
]

[start]
cmd = "./start.sh"
